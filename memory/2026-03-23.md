# 2026-03-23

- Pulled latest `main` to `8e5a479`.
- Reduced first-adoption friction in `adopt_governance.py`:
  - existing `contract.yaml` is still kept instead of being overwritten
  - but adopt now auto-repairs one framework-required seam when missing
  - if neither `documents` nor `ai_behavior_override` references `AGENTS.base.md`, adopt adds `AGENTS.base.md` to `ai_behavior_override`
- Added a baseline CI drift workflow seed:
  - `baselines/repo-min/.github/workflows/governance-drift.yml` now provides a minimal GitHub Actions gate for `governance_drift_checker.py`
  - adopt copies that workflow into target repos only when they do not already have their own `governance-drift.yml`
- Updated adoption documentation:
  - README no longer claims a fixed `15/16` post-adoption check expectation
  - `baselines/repo-min/README.md` now documents exactly which files the minimal baseline seeds and which ones are always preserved vs copied-only-when-missing
- Improved the `AGENTS.md` baseline template:
  - added a short quick-start section so first-time adopters know they only need to fill the highest-risk repo-specific rules first
  - expanded the per-section comment prompts so teams can translate existing repo conventions into `risk_levels`, `must_test_paths`, `escalation_triggers`, and `forbidden_behaviors` more directly
- Improved adopt post-run guidance:
  - `adopt_governance.py` now points out which `AGENTS.md` repo-specific sections are still at template `N/A` defaults
  - this keeps the first follow-up action concrete without changing drift semantics, since `N/A` remains valid for minimal/prototype repos
- Improved refresh post-run guidance:
  - `adopt_governance.py --refresh` now prints a small delta summary after rewriting `.governance/baseline.yaml`
  - the summary calls out tracked governance files whose hashes changed and PLAN heading inventory sections that were added or removed
- Prepared Step 7 output-tier separation changes for push:
  - added `governance_tools/output_tier.py` and `governance_tools/reasoning_compressor.py`
  - threaded task-level-aware tier output into `pre_task_check.py` and `session_start.py`
  - aligned baseline `AGENTS.md` placeholders and minimum-legal-schema docs around plain `N/A`
  - updated drift checker expectations so fresh baseline `AGENTS.md` passes with plain `N/A`
- Added focused tests for the repair path and dry-run behavior in `tests/test_adopt_governance.py`.
- Added focused tests that verify the drift workflow is created when missing and preserved when already customized.
- Verification:
  - `python -m py_compile governance_tools/adopt_governance.py tests/test_adopt_governance.py` passed
  - `pytest -q tests/test_adopt_governance.py -k "repairs_existing_contract_with_missing_agents_base_reference or dry_run_does_not_repair_existing_contract"` passed (`2 passed`)
  - `pytest -q tests/test_adopt_governance.py -k "creates_governance_drift_workflow_when_missing or keeps_existing_governance_drift_workflow or repairs_existing_contract_with_missing_agents_base_reference"` passed (`3 passed`)
  - `pytest -q tests/test_adopt_governance.py -k "refresh_reports_changed_tracked_hashes or refresh_reports_plan_inventory_delta or surfaces_repo_specific_agents_prompts_in_next_steps"` passed (`3 passed`)
  - `pytest -q tests/test_reasoning_compressor.py` passed (`57 passed`)
  - manual verification confirmed Tier 3 output artifact emission and `agents_sections_filled=True` for baseline `AGENTS.md` with plain `N/A`
- Added a Step 7 post-roadmap rebaseline guide:
  - `docs/payload-audit/step7-rebaseline-checklist.md` now defines the rerun checklist for `L0`, `L1`, and `Onboarding`
  - it also includes a copy-paste recording template so fresh token baselines can be captured consistently
- Ran the first Step 7 rebaseline measurements against the framework repo itself:
  - `L0` UI run (`Update button label in UI`) produced `combined_estimate=6550`
    - `output_tier=1`
    - `domain_contract=null`
    - still effectively flat versus the prior `~6451` baseline
  - `L1` implementation run (`Refactor state generator module`) produced `combined_estimate=21564`
    - slightly above the prior `~20539` baseline
    - main fields remained `pre_task_check`, `domain_contract`, `state`, `change_proposal`
  - `Onboarding`-shaped run (`Adopt governance baseline for external repo`) produced `combined_estimate=21088`
    - still logged into `L1-2026-03-23.jsonl` because the runtime logger keys by explicit `task_level`
    - did not create a separate `onboarding-*.jsonl` record in this path
  - practical interpretation:
    - Step 7 clearly preserves the low L0 budget
    - Step 7 has not yet reduced L1/onboarding token cost by itself
    - the remaining biggest leverage is still onboarding/domain-summary shaping rather than output-tier mechanics alone
  - execution note:
    - `session_start.py` hit a Windows `cp950` `UnicodeEncodeError` when printing large JSON with emoji-like characters
    - the audit record was still written before the terminal encoding failure
    - using `PYTHONIOENCODING=utf-8` avoided that output failure for the onboarding-shaped rerun

- Wrote the Step 7 rebaseline numbers back into the payload-audit docs:
  - `docs/payload-audit/L0-baseline.md`
  - `docs/payload-audit/L1-baseline.md`
  - `docs/payload-audit/onboarding-baseline.md`
- Added a standalone Step 1-Step 7 token summary:
  - `docs/payload-audit/step1-step7-token-summary.md`
  - records both:
    - observed total reduction: `104696 -> 49202` (`-55494`, `-53.0%`)
    - strict comparable reduction (`L0 + L1` only): `44073 -> 28114` (`-15959`, `-36.2%`)
