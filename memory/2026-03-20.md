# 2026-03-20

- Added a v2.6 governance-runtime design note and machine-readable draft under
  `docs/governance-runtime-v2.6.md` and
  `governance/governance_decision_model.v2.6.json`.
- The draft formalizes the enforcement model, decision ownership, policy
  precedence, evidence classification, violation handling, and determinism
  contract.
- The contract-repo boundary is now written down explicitly: preserve domain
  assets, but move verdict ownership and fallback behavior into the framework
  runtime.
- Recorded user feedback about governance friction in real collaboration:
  - validation tooling is fragile and expensive to maintain; many scripts are
    tightly coupled to implementation details, so small renames can break
    multiple validators
  - phase gates are too heavy for UI and exploratory work; agents often need to
    implement first and backfill governance artifacts later
  - multiple behavior contracts such as `AGENTS.md` and `governance/AGENT.md`
    create cognitive load and possible rule conflicts
  - schema changes trigger broad ripple effects across models, DTOs, APIs,
    frontend, and validators, causing repeated infrastructure-repair loops
  - long governance documents consume too much context budget and degrade room
    for code reasoning
- Recorded improvement directions from the user:
  - tiered governance, with L0 fast-track for UI and prototype work and stricter
    governance for core domain logic
  - decoupled validation focused on interface/schema outcomes rather than string
    scans or private implementation details
  - consolidated truth by reducing overlapping agent/governance documents and
    removing redundant backup artifacts
- Started turning that feedback into a formal status memo so the next governance
  simplification changes are driven by an explicit problem statement rather than
  only daily notes.

## Governance friction backlog

- Validation fragility:
  - more than 50 governance scripts now exist in `governance_tools/`
  - rename-level changes can break several validators at once
  - next action: audit validators that depend on file names, wording, or private
    implementation details
- Phase-gate weight:
  - `Analyze -> Define -> Test -> Implement` is too heavy for exploratory UI and
    layout work
  - next action: define a true L0 fast-track path for prototype and
    presentation-heavy work
- Document overlap:
  - `AGENTS.md` and `governance/AGENT.md` both act like live behavior contracts
  - next action: choose one canonical contract and reduce the other to a
    reference boundary
- Schema ripple effect:
  - additive model changes still force broad synchronized updates across DTOs,
    APIs, frontend, and validators
  - next action: define compatibility rules for additive schema changes and
    shift validators toward versioned normalized envelopes
- Context cost:
  - long governance docs consume too much context before implementation starts
  - next action: reduce default governance payload size and load deeper policy on
    demand

## Review notes on commit c71e0c1

- User review: commit `c71e0c1` clearly responds to the previously identified
  architecture gaps, but mostly at the definition/specification layer rather
  than full runtime implementation.
- What the review considers meaningfully addressed:
  - mixed enforcement is now described as a transitional seam rather than the
    final architecture
  - runtime is explicitly defined as the single decision computation source
  - three existing matrices were upgraded and a violation-handling matrix was
    added as machine-checkable spec
  - policy precedence is now explicit rather than implicit
  - evidence handling now includes validity/trust dimensions, not just presence
  - violation handling is modeled for abnormal/error states
  - a determinism contract now exists
  - integration boundaries for external contract repos are explicitly split into
    preserve/rewire/keep-out areas
- What the review considers still incomplete:
  - the change is still a decision-model draft, not a full runtime rewrite
  - decision ownership is clearer for computation and partial authority, but not
    yet fully modeled across computation/authority/presentation as separate
    sources
  - evidence flow is moving toward a push-first envelope model, but provenance
    fields and a full envelope contract are not yet fully specified
  - trace requirements are acknowledged, but a concrete machine/human trace
    schema is not yet present
  - verdict lifecycle/state-machine behavior is still not fully defined
- User assessment:
  - the commit is worth doing because it stabilizes the control plane for future
    runtime refactors
  - it helps control Kernel/USB contract impact by explicitly documenting the
    preserve vs rewire boundary
  - it does not yet complete execution-layer migration
- User-specified next validation targets:
  - runtime should start reading `governance/governance_decision_model.v2.6.json`
  - hooks/CI/validator paths should move final verdict ownership into runtime
  - trace artifacts and verdict artifacts should become formal outputs
  - legacy `hard_stop_rules` should be downgraded into policy input rather than
    continuing as a direct outcome switch

- Implemented the first governance-friction reduction slice:
  - formalized `L0 fast-track` as a real lightweight path in
    `governance/AGENT.md`, `governance/ARCHITECTURE.md`,
    `governance/TESTING.md`, and `governance/SYSTEM_PROMPT.md`
  - expanded `L0` to cover bounded presentation-only and prototype UI shaping
    without forcing full `L1+` phase-gate ceremony
  - added explicit upgrade triggers from `L0` to `L1` for schema, behavior,
    shared logic, ambiguity, or heavier verification needs
  - clarified single-truth boundaries between root `AGENTS.md` and
    `governance/AGENT.md`: workspace behavior vs repo engineering governance

- Implemented the next schema/validator stabilization slice:
  - `governance_tools/validator_interface.py` now declares stable payload/result
    schema versions
  - `governance_tools/domain_validator_loader.py` now emits a versioned
    `domain-validator-payload` envelope with `contract_snapshot`,
    `evidence_envelope`, provenance fields, and additive-compatibility metadata
  - legacy top-level payload fields remain preserved for backward compatibility
  - added tests asserting the new envelope shape and validator visibility of the
    versioned metadata

## Review notes on commit f356569

- User review: `f356569` is a meaningful step from draft/spec toward a more
  operational seam, especially for governance-friction reduction and domain
  validator payload stability.
- What the review considers clearly improved:
  - root `AGENTS.md` vs `governance/AGENT.md` boundary is now explicitly split
    into workspace behavior vs repo governance
  - `L0 fast-track` is now more real and more bounded, with explicit forbidden
    areas and immediate upgrade triggers
  - validator payloads now behave more like a formal integration contract:
    versioned, enveloped, provenance-aware, and backward-compatible
  - README now reflects the validator seam as versioned payload envelopes with
    legacy field preservation
- What the review still considered open at the time:
  - runtime did not yet prove a dedicated lightweight L0 runtime path
  - runtime-to-loader plumbing for `contract_file=resolved_contract_file` still
    looked incomplete
  - trace artifact / verdict artifact outputs were still missing as first-class
    runtime outputs
- Important follow-up state:
  - the runtime plumbing concern was resolved immediately afterward in commit
    `b26e41b`, which passes `resolved_contract_file` into the validator runtime
    envelope so `contract_path` is now populated automatically
- User-recommended next order remains:
  - first ensure runtime plumbing is complete end-to-end
  - then add the smallest viable trace/verdict artifact rather than designing a
    large output format first

- Implemented the next minimal runtime-output slice:
  - `runtime_hooks/core/session_end.py` now emits first-class
    `artifacts/runtime/verdicts/<session_id>.json` and
    `artifacts/runtime/traces/<session_id>.json`
  - verdict artifact includes schema version, policy ref, contract identity,
    evidence summary, and override/escalation presence
  - trace artifact includes schema version, policy ref, contract identity,
    runtime contract, decision path, evidence summary, and final result bundle

- Started the next trust-validation slice around failure behavior:
  - added `docs/failure-mode-test-plan.md` as the first deliberate break-test
    plan for the runtime
  - added `governance/failure_mode_test_matrix.v0.1.json` as a
    machine-readable checklist covering:
    - `missing_required_evidence`
    - `invalid_evidence_schema`
    - `policy_conflict`
    - `runtime_failure`
    - `determinism_replay`
  - added `tests/test_failure_mode_test_matrix.py` to prevent these first
    failure-mode scenarios from silently disappearing
  - this does not yet prove the runtime passes these scenarios; it fixes the
    test contract and scope for the next execution-layer work

- Implemented the first execution-layer failure-mode check in `runtime_hooks/core/post_task_check.py`:
  - runtime now loads required evidence kinds from `governance/governance_decision_model.v2.6.json`
  - added machine-readable `evidence_violations` output for `missing_required_evidence`
  - `checks.required_runtime_evidence` can now trigger a concrete runtime classification instead of only relying on prose or future plans
  - the first concrete verdict impact wired in is `missing_required_evidence -> escalate`
- Added runtime coverage in `tests/test_runtime_post_task_check.py`:
  - `test_post_task_check_classifies_missing_required_runtime_evidence` now asserts the new machine-readable violation record and human summary count
- Verification:
  - `pytest -q tests/test_runtime_post_task_check.py -k "missing_required_runtime_evidence"` passed
  - existing versioned validator envelope path was manually re-checked because this environment still intermittently blocks `pytest tmp_path` creation

- Implemented the second execution-layer failure-mode check in `runtime_hooks/core/post_task_check.py`:
  - added `invalid_evidence_schema` classification for malformed `public_api_diff` evidence
  - current runtime behavior is now:
    - missing required runtime evidence -> `escalate`
    - invalid public-api-diff evidence schema -> `stop`
  - malformed evidence is treated as present-but-invalid, so it no longer double-classifies as both invalid and missing
- Added runtime coverage in `tests/test_runtime_post_task_check.py`:
  - `test_post_task_check_classifies_invalid_public_api_diff_schema`
  - together with the previous missing-evidence test, this now gives the runtime its first two concrete failure-mode executions
- Verification:
  - `pytest -q tests/test_runtime_post_task_check.py -k "missing_required_runtime_evidence or invalid_public_api_diff_schema"` passed (`2 passed`)

- Implemented the third execution-layer failure-mode slice in `runtime_hooks/core/post_task_check.py`:
  - runtime now loads `policy_precedence` rows from `governance/governance_decision_model.v2.6.json`
  - added machine-readable `policy_violations` output
  - current policy classification behavior is now:
    - allowed precedence conflict -> `policy_conflict` with `verdict_impact = escalate`
    - forbidden precedence override -> `illegal_override` with `verdict_impact = stop`
  - human output now includes `policy_violation_count`
- Added runtime coverage in `tests/test_runtime_post_task_check.py`:
  - `test_post_task_check_classifies_policy_conflict_from_precedence_matrix`
  - `test_post_task_check_classifies_illegal_policy_override`
- Verification:
  - `pytest -q tests/test_runtime_post_task_check.py -k "policy_conflict_from_precedence_matrix or illegal_policy_override"` passed (`2 passed`)

- Implemented the fourth execution-layer failure-mode slice in `runtime_hooks/core/session_end.py`:
  - added a minimal fail-closed runtime failure path for artifact emission
  - `checks.force_runtime_failure_stage = "artifact_emission"` now forces a controlled runtime exception for testing
  - runtime intercepts that exception, flips the result to `decision = RUNTIME_FAILURE`, sets `policy.decision = STOP`, and emits a `runtime-failure-trace` artifact instead of propagating the exception outward
- Added runtime coverage in `tests/test_runtime_session_end.py`:
  - `test_session_end_fails_closed_on_forced_runtime_failure`
- Verification:
  - `python -m py_compile runtime_hooks/core/session_end.py` passed
  - `pytest -q tests/test_runtime_session_end.py -k "forced_runtime_failure"` passed (`1 passed`)

- Implemented the first minimal determinism-replay slice:
  - `runtime_hooks/core/session_end.py` now includes `policy_ref.runtime_version = "v2.6-draft-runtime"` in verdict/trace artifacts, matching the v2.6 determinism contract requirement that runtime version be traced
  - added replay coverage for both `post_task_check` and `session_end`
- Added tests:
  - `test_post_task_check_replay_is_deterministic_for_same_policy_and_evidence`
  - `test_session_end_replay_preserves_verdict_for_same_input`
- Verification:
  - `python -m py_compile runtime_hooks/core/session_end.py` passed
  - `pytest -q tests/test_runtime_post_task_check.py -k "replay_is_deterministic"` passed (`1 passed`)
  - `pytest -q tests/test_runtime_session_end.py -k "replay_preserves_verdict"` passed (`1 passed`)
- This is still the first deterministic slice, not a full replay harness:
  - it proves stable verdicts and violation classification for fixed synchronous inputs
  - it does not yet prove determinism across async ordering, external tool jitter, or human override replay

- Promoted the failure-mode work from memory into formal status docs:
  - added `docs/status/runtime-failure-mode-progress-2026-03-20.md`
  - updated `docs/status/README.md` to include it in the recommended reading path
  - updated `docs/releases/v1.0.0-alpha.md` so the alpha release note now reflects that failure handling is no longer only a spec-layer claim

- Rewired legacy `hard_stop_rules` to behave more like policy input than a direct validator-side outcome switch:
  - `runtime_hooks/core/post_task_check.py` no longer writes matched hard-stop validator violations straight into `errors` during the validator merge step
  - domain validator findings now remain visible as advisory warning surface
  - matched `hard_stop_rules` are converted into machine-readable runtime `policy_violations` with:
    - `violation_type = domain_contract_violation`
    - `policy_type = domain-contract policy`
    - `source = hard_stop_rules`
    - `verdict_impact = stop`
  - runtime still blocks the task, but the blocking owner is now the runtime policy layer rather than the validator merge path
- Updated the existing hard-stop test expectation in `tests/test_runtime_post_task_check.py` to reflect the new seam
- Verification:
  - `python -m py_compile runtime_hooks/core/post_task_check.py` passed
  - manual runtime validation under a repo-local writable temp root confirmed:
    - validator violation remains in warnings
    - stop behavior is now expressed through `policy_violations`
    - final error surface is `runtime-policy: ...`

- Synced the main documentation entry points with the new `hard_stop_rules` behavior:
  - `README.md` now describes selected rule IDs as runtime policy input rather than direct hard-stop escalation
  - `runtime_hooks/README.md` now describes the validator seam as advisory-first with runtime policy reclassification
  - `docs/status/runtime-failure-mode-progress-2026-03-20.md` now reflects that `hard_stop_rules` still exist as metadata, but no longer act as a direct validator-side outcome switch

- Recorded an external collaboration analysis about `ai-governance-framework`
  × `Kernel-Driver-Contract` integration problems:
  - structural incompatibility:
    - governance contract `LANG` enum does not include `C`, which blocks
      contract-compliant output for real kernel-driver codebases
    - memory schema expectations diverge between framework-side
      `memory/02_tech_stack.md` style assumptions and contract-side
      `memory/02_project_facts.md` / `03_decisions.md` / `04_validation_log.md`
  - protocol gap:
    - VS Code Copilot flows did not emit the required `[Governance Contract]`
      block before non-trivial analysis, so the interaction would be
      non-compliant from the framework's own perspective
  - authority conflict:
    - workspace `copilot-instructions.md` and framework `SYSTEM_PROMPT.md`
      both effectively claim top-rank authority without a clean conflict
      resolution path
  - integration workflow incompleteness:
    - `Kernel-Driver-Contract` phase-2 real-repo integration remains incomplete
    - `memory/02_project_facts.md` is still pending despite real GlPciSD facts
      already existing elsewhere
    - `scan_source.py` / `run_validators.py` have not yet been replayed against
      the real GlPciSD source tree
  - summarized issue list captured from the analysis:
    - `LANG` enum missing `C`
    - memory schema mismatch
    - governance-contract block not enforced in Copilot flow
    - authority hierarchy conflict between workspace instructions and framework
      prompt
    - unsynced fact stores
    - validator toolchain not yet run on the real adopter repo
    - `SCOPE` enum still weak for kernel-driver-specific work
    - `WORKFLOW.md` contains at least one stale path example

- Started resolving the first concrete structural blocker from the
  `ai-governance-framework` × `Kernel-Driver-Contract` collaboration analysis:
  - governance contract `LANG` now accepts `C` in addition to the previous
    language set
  - updated framework prompt and validator expectations so C-based kernel-driver
    repositories can produce a contract-compliant `[Governance Contract]` block

- Centralized the v2.6 decision-model read path into `governance_tools/decision_model_loader.py`:
  - runtime now has one shared loader for model version, policy ref, required evidence kinds, policy precedence rows, and violation verdict impacts
  - `post_task_check.py` no longer reads the JSON model directly in multiple local helpers
  - `session_end.py` no longer hardcodes `governance_runtime_decision_model = 2.6-draft`; it now builds `policy_ref` from the shared loader
- Runtime alignment improvements from this slice:
  - violation impacts in `post_task_check.py` are now sourced from the model's `violation_handling` matrix rather than local hardcoded strings
  - verdict/trace artifacts now point at the decision model version from the shared loader while preserving `runtime_version` tracing
- Added `tests/test_decision_model_loader.py`
- Verification:
  - `python -m py_compile governance_tools/decision_model_loader.py runtime_hooks/core/post_task_check.py runtime_hooks/core/session_end.py` passed
  - `pytest -q tests/test_decision_model_loader.py` passed (`2 passed`)
  - `pytest -q tests/test_runtime_post_task_check.py -k "replay_is_deterministic or missing_required_runtime_evidence or invalid_public_api_diff_schema or policy_conflict_from_precedence_matrix or illegal_policy_override"` passed (`5 passed`)
  - `pytest -q tests/test_runtime_session_end.py -k "forced_runtime_failure or replay_preserves_verdict"` passed (`2 passed`)

- Extended the shared decision-model loader usage into artifact-level decision identity:
  - `governance_tools/decision_model_loader.py` now also exposes:
    - `runtime_decision_source()`
    - `final_verdict_owner()`
  - `runtime_hooks/core/session_end.py` now emits `decision_governance` in verdict/trace artifacts, sourced from the v2.6 decision model:
    - `decision_source = single decision computation source`
    - `decision_owner = runtime`
- Added/updated tests:
  - `tests/test_decision_model_loader.py`
  - `tests/test_runtime_session_end.py`
- Verification:
  - `python -m py_compile governance_tools/decision_model_loader.py runtime_hooks/core/session_end.py` passed
  - `pytest -q tests/test_decision_model_loader.py` passed (`3 passed`)
  - `pytest -q tests/test_runtime_session_end.py -k "forced_runtime_failure or preserves_contract_context"` passed (`2 passed`)

- Upgraded `runtime_hooks/core/session_end.py` trace output from string-only summaries toward structured reasoning fragments:
  - `decision_path` is now emitted as ordered step objects instead of plain strings
  - `decision_governance` now also includes `policy_source = memory_pipeline.promotion_policy.classify_promotion_policy`
  - trace `result.policy` is now normalized into a structured object with:
    - `decision`
    - `reason_count`
    - `reasons`
    - `reasoning_fragments`
  - runtime failure trace now also emits a structured policy object with a `runtime-failure` reasoning fragment
- Updated `tests/test_runtime_session_end.py` to assert the new structured reasoning output
- Verification:
  - `python -m py_compile runtime_hooks/core/session_end.py` passed
  - `pytest -q tests/test_runtime_session_end.py -k "auto_promotes_low_risk_candidate or forced_runtime_failure or replay_preserves_verdict"` passed (`3 passed`)

- Started resolving the second structural blocker from the same collaboration analysis:
  - added alias-aware memory layout resolution so the framework can recognize external-domain repo schemas such as:
    - `02_project_facts.md`
    - `03_decisions.md`
    - `04_validation_log.md`
  - `memory_pipeline/memory_promoter.py` now reuses those aliases when present instead of always forcing canonical `03_knowledge_base.md` / `04_review_log.md`
  - `governance/SYSTEM_PROMPT.md` now explicitly documents those accepted aliases


- Added a minimal adapter-side Governance Contract enforcement slice in `runtime_hooks/adapters/shared_adapter_runner.py`:
  - non-trivial `post_task` runs now treat `[Governance Contract]` presence as required
  - missing contract blocks are surfaced as explicit `adapter-contract` errors
  - adapter results now include an `adapter_contract` assessment payload with `required`, `contract_found`, `compliant`, `errors`, and `warnings`
- Added `tests/test_runtime_adapter_runner.py::test_run_adapter_event_post_task_reports_missing_contract`
- Verification:
  - `python -m py_compile runtime_hooks/adapters/shared_adapter_runner.py tests/test_runtime_adapter_runner.py` passed
  - `pytest -q tests/test_runtime_adapter_runner.py` passed (`4 passed`)


- Expanded governance contract `SCOPE` support for external driver collaboration:
  - `governance_tools/contract_validator.py` now accepts `governance` and `kernel-driver`
  - `governance/SYSTEM_PROMPT.md` scope enum now matches the validator
- Verification:
  - `python -m py_compile governance_tools/contract_validator.py tests/test_contract_validator.py` passed
  - `pytest -q tests/test_contract_validator.py -k "valid_scope"` passed (`8 passed`)


- Clarified authority hierarchy for external integrations:
  - `AGENTS.md` now explicitly says editor/adapter/workspace instructions do not override repo governance on execution rigor, risk gates, or task classification
  - `governance/SYSTEM_PROMPT.md` now defines an `External Instruction Boundary` so workspace prompts and harness-native guidance are treated as transport/session instructions, not alternate engineering constitutions
  - `governance/AGENT.md` now mirrors that boundary from the repo-governance side
- Also fixed `SYSTEM_PROMPT.md` field rules so `SCOPE` documentation matches validator support for `kernel-driver`


- Aligned onboarding and external-integration status docs with the current runtime seam:
  - `README.md` onboarding section now describes the full onboarding flow as hook install + readiness + smoke + report artifact output, and uses generic path placeholders plus a concrete `Kernel-Driver-Contract` example command
  - `docs/status/four-repo-integration-progress.md` now describes external enforcement as runtime policy-input reclassification instead of older mixed-enforcement shorthand
  - `docs/status/domain-enforcement-matrix.md` now uses portable `/path/to/...` examples and explains `hard_stop_rules` as runtime policy reclassification


- Added a minimal external fact-store intake seam:
  - new tool `governance_tools/external_project_facts_intake.py`
  - resolves external `memory/02_project_facts.md` or `memory/02_tech_stack.md`
  - emits a framework-side `artifacts/external-project-facts/<repo>.json` artifact with provenance (`source_file`, `source_filename`, `content_sha256`, `sync_direction=external_to_framework`)
- Added `tests/test_external_project_facts_intake.py`
- Verification:
  - `python -m py_compile governance_tools/external_project_facts_intake.py tests/test_external_project_facts_intake.py` passed
  - `pytest -q tests/test_external_project_facts_intake.py` passed (`3 passed`)


- Integrated external `project_facts` intake into onboarding/readiness surfaces:
  - `external_repo_readiness.py` now detects and reports `project_facts_present` / `project_facts_intakeable`
  - readiness JSON/human output now includes a `project_facts` section with `source_file`, `source_filename`, and `sync_direction`
  - `external_repo_onboarding_report.py` now carries that same `project_facts` metadata into onboarding reports and bundles
- Verification:
  - `python -m py_compile governance_tools/external_repo_readiness.py governance_tools/external_repo_onboarding_report.py tests/test_external_repo_readiness.py tests/test_external_repo_onboarding_report.py` passed
  - `pytest -q tests/test_external_repo_readiness.py tests/test_external_repo_onboarding_report.py` passed (`10 passed`)

