# 2026-03-16

- Continued the semantic-verification track by deepening `governance_tools/public_api_diff_checker.py`.
- Improved C# signature normalization:
  - normalize generic/nullability spacing in type names
  - split parameter lists on top-level commas instead of every comma
  - preserve cleaner semantic metadata for methods and constructors
- Replaced line-based C# property parsing with brace-aware extraction so multiline properties and accessor blocks are recognized.
- Refined C# property compatibility classification:
  - getter/setter/init removal is treated as breaking
  - additive accessor expansion such as `get -> get/set` is treated as non-breaking
  - property type changes remain breaking
- Adjusted raw-signature diff handling so C# semantic coverage can override false breaking signals caused by header-text churn, while uncovered signatures still fall back to generic added/removed warnings.
- Added regression coverage in `tests/test_public_api_diff_checker.py` for:
  - multiline property signature normalization
  - property setter addition as a non-breaking change
- Continued the same public-API track by making C# semantic identities namespace-aware.
- Added scope resolution for:
  - block namespaces
  - file-scoped namespaces
  - qualified type containers used by methods, properties, and constructors
- This closes a real false-negative case where `Alpha.Service` and `Beta.Service` could collapse into the same semantic identity and hide a breaking change in only one namespace.
- Added regression coverage for:
  - namespace-qualified manifest identities
  - same-type-name collisions across multiple namespaces
- Continued the same semantic-verification track by adding generic-aware C# API normalization.
- `public_api_diff_checker.py` now recognizes generic type declarations and generic methods instead of treating them as raw-signature-only lines.
- Generic declarations are canonicalized to stable placeholders, so renaming `TItem -> TValue` or `TResult -> TOut` no longer creates false breaking API diffs when the actual surface is unchanged.
- Semantic coverage of raw signatures is now prefix-aware, which lets type headers with `{` and expression-bodied methods with `=> ...;` be suppressed by semantic normalization rather than re-flagged by the raw fallback layer.
- Added regression coverage for:
  - generic type + method signature normalization
  - generic parameter rename as a non-breaking change
- Verification:
  - `tests/test_public_api_diff_checker.py` -> `14 passed`
  - `tests/test_architecture_impact_estimator.py` -> `4 passed`
  - `tests/test_change_proposal_builder.py tests/test_runtime_pre_task_check.py` -> `13 passed`
  - `tests/test_architecture_impact_estimator.py tests/test_change_proposal_builder.py tests/test_runtime_pre_task_check.py` -> `17 passed`
